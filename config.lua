Config = {
    General = {
        updateInterval = 1000,          -- 1000 = 1 second (It is not recommended to set it to 0.)
        playerBlip = true,              -- If the value is "true" the player's own blip remains closed, but if the value is "false" the player's blip remains open. (Coming Soon)
        itemName = 'gps',               -- Item Name
    },
}

-- For Blip Information: https://docs.fivem.net/docs/game-references/blips/